# ADR-NNN: <short slug matching filename>

**Status:** proposed
**Date:** YYYY-MM-DD
**Deciders:** <agents / humans involved — e.g. tclancy, agent:opus>

<!-- Legal Status values:
       proposed    — under discussion; work referencing this MUST NOT merge
       accepted    — decided; work may proceed
       superseded  — replaced by another ADR (name it in "Superseded by")

     A `blocked-by-decision` label on issues or PRs referencing this ADR
     while it is `proposed` tells the PM sweep to keep it out of the merge
     queue. See agent.md → Vocabulary and Decisions.
-->

## Context

<What forced this decision? The problem, constraint, or fork in the road.
Two or three sentences, no more. Link the issue or PR that surfaced it.>

## Decision

<What was chosen, in one paragraph. Prefer active voice: "we index by X"
not "X is used as the index." Name the mechanism, not the goal.>

## Consequences

- **Enables:** <one line — what this makes possible.>
- **Blocks / makes harder:** <one line — what this closes off. If nothing,
  omit; but usually there is something and hiding it costs later.>
- **Reverses if:** <one line — the condition under which we'd revisit this
  decision. If none, write "N/A — one-way door.">

## Alternatives considered

<!-- Two or three named alternatives, each with a one-line rejection reason.
     Cheap to write, expensive to leave out — future readers ask "why not
     X?" and the answer needs to survive the ADR author. -->

- **Option A:** why not.
- **Option B:** why not.

## Superseded by

<!-- Fill in when this ADR is retired. Otherwise remove this heading. -->
